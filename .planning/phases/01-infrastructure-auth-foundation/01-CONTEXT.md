# Phase 1: Infrastructure & Auth Foundation - Context

**Gathered:** 2026-03-15
**Status:** Ready for planning

<domain>
## Phase Boundary

Stand up all infrastructure services via Docker Compose, build the FastAPI API gateway with JWT auth, create the Next.js web shell with i18n, and set up the LLM Gateway and Temporal foundation. This phase delivers a running platform where users can register, log in, and see a web application shell.

</domain>

<decisions>
## Implementation Decisions

### Project Structure
- Python monorepo with `backend/` (FastAPI + agents + data sources) and `apps/web/` (Next.js)
- `apps/desktop/` (Tauri) deferred to Phase 8
- `shared/` for cross-project type definitions
- `infra/` for Docker Compose and configuration
- Use pnpm workspaces for frontend, uv/pyproject.toml for Python backend
- Monorepo root has scripts for `dev`, `build`, `test` across all packages

### Infrastructure Configuration
- Docker Compose for all services: PostgreSQL 17, Neo4j 2025.01+, Meilisearch 1.37, Valkey 8.1+, SeaweedFS, Temporal Server 1.25+
- `.env` file for all configuration (ports, credentials, API keys)
- Health check endpoints for every service
- Development mode: hot reload for both FastAPI (uvicorn --reload) and Next.js
- All services start with `docker compose up` — single command

### Auth Flow
- Email + password registration (no OAuth for v1)
- No email verification required for MVP — professors will be invited directly
- JWT access token (short-lived, 15 min) + refresh token (long-lived, 7 days)
- Passwords hashed with bcrypt
- Session persists across browser refresh via refresh token rotation
- No admin role distinction for MVP — all users are researchers

### i18n Approach
- Default language: Chinese (zh-CN) — primary users are Chinese professors
- English as secondary language
- next-intl for Next.js i18n
- UI labels in both languages, content (paper titles, abstracts) displayed in original language
- Language toggle in header, preference saved in user profile

### LLM Gateway
- Unified interface supporting Claude and OpenAI models
- Cost tracking per request and per user
- Model fallback: if primary model fails, try secondary
- Rate limiting per user to control costs
- Configuration via environment variables (API keys, default model, max tokens)

### Temporal Foundation
- Temporal Server in Docker Compose
- Python SDK for workflow/activity definitions
- Basic workflow template for Deep Research (expanded in Phase 5)
- Workflow UI (Temporal Web) available in dev mode

### Claude's Discretion
- Exact port assignments for services
- Database schema migration tool choice (Alembic recommended)
- FastAPI project structure (routers, services, models organization)
- Next.js app router structure
- Logging format and level configuration
- Error response format
- CORS configuration
- Rate limiting strategy (token bucket, sliding window, etc.)

</decisions>

<specifics>
## Specific Ideas

- Design doc proposes the full project structure at `docs/plans/2026-03-15-studyhub-design.md` — follow that as reference but adapt based on research findings
- Stack research validated: use Valkey instead of Redis, SeaweedFS instead of MinIO (archived Feb 2026)
- First users are ECG research professors — they will be onboarded manually, no public registration flow needed yet
- Self-hosted deployment on server with GPU — Docker Compose must be production-capable, not just dev

</specifics>

<deferred>
## Deferred Ideas

- OAuth login (Google, GitHub) — future enhancement
- Email verification flow — add when public registration opens
- Admin dashboard — defer until needed
- API documentation (Swagger/ReDoc) — auto-generated by FastAPI, but custom docs page deferred
- Tauri desktop app — Phase 8

</deferred>

---

*Phase: 01-infrastructure-auth-foundation*
*Context gathered: 2026-03-15*
