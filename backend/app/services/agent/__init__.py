"""Agent Runtime — Plan → Approve → Execute workflow for document generation.

Inspired by Kuse Cowork's agent loop architecture, adapted for StudyHub's
Python backend. The agent takes deep research results as context and
generates documents (literature reviews, experiment plans) via LLM.
"""
